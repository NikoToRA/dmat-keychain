{
  "name": "dmat-keychain-ec",
  "version": "1.0.0",
  "description": "DMAT キーホルダー EC サイト",
  "scripts": {
    "dev": "npx @azure/static-web-apps-cli start src --api-location api",
    "start": "npx @azure/static-web-apps-cli start src --api-location api"
  },
  "devDependencies": {
    "@azure/static-web-apps-cli": "^2.0.0"
  }
}
